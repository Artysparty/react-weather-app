import React from 'react';

import s from './Month-statistics.module.scss';

interface Props {};

export const MonthStatistics = (props: Props) => {
  return (
    <div className={s.monthStatistics}>
      Month Statistics
    </div>
  )
}